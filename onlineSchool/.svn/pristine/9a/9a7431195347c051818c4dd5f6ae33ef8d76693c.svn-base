
## 用FreeMarker标签实现页面重构（二）
一、实现页面重构
    1、不同菜单做页面跳转
    2、菜单的背景色设置
    3、完成页面重构功能
    
二、FreeMarker 标签技术点
    1、<@extends name="_manage_base.html"/> 继承父页面
    2、<@override name="title"> </@override> 覆盖父页面内容
    3、<#include "_manage_nav.html"/> 包含页面
    4、<#assign >语法、${var}语法、<#if >语法 
    
三、代码实战（文件）
    1、添加FreeMarker的 extends、override、include 的标签支持
        修改pom.xml，引入包
        <dependency>
            <groupId>com.googlecode.rapid-framework</groupId>
            <artifactId>rapid-core</artifactId>
            <version>4.0.5</version>
        </dependency>
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <version>2.6</version>
        </dependency>
    2、添加FreeMarker的Config类
    3、重构页面：每一个页面
    
## 祁大聪讲编程

